datasource db {
  url      = env("DATABASE_URL")
  provider = "sqlite"
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-arm-openssl-1.1.x"]
}

model BadUser {
  id       String @id
  username String
}
