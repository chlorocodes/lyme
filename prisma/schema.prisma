datasource db {
  url      = env("DATABASE_URL")
  provider = "sqlite"
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native"]
}

model BadUser {
  id       String @id
  username String
}
