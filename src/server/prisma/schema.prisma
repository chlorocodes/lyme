generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DB_URL")
}

model User {
  id       String @id @map("id")
  username String @unique @map("username")
}
