generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DB_URL")
}

model Session {
  id       String  @id @map("id")
  userId   String? @map("user_id")
  username String? @map("username")

  @@map("sessions")
}
